# Dependency pipeline Envirocircle, dipakai Atmosight DAN Smokewatch.
#
# Dulu dua berkas terpisah. Digabung sebab setelah kedua repo disatukan,
# dua venv untuk satu pohon cuma bikin bingung. Yang dipakai versi terkunci
# milik Atmosight, sebab dia yang paling ketat, dan seluruh syarat Smokewatch
# (numpy>=2.0, xarray>=2024.1, netCDF4>=1.6, pillow>=10, requests>=2.28)
# sudah terpenuhi oleh angka angka di bawah ini.
#
# Diperiksa dengan mendata seluruh import di kedua pipeline. Yang dari pustaka
# pihak ketiga cuma enam, contourpy, netCDF4, numpy, pillow, requests, xarray.
# CAMS TIDAK butuh cdsapi, dia diambil lewat requests biasa ke Atmosphere
# Data Store.
numpy==2.5.1
xarray==2026.7.0
pillow==12.3.0
requests==2.34.2
netCDF4==1.7.4     # baca NetCDF, dipakai CAMS dan nc WRF Indonesia
contourpy==1.3.0   # kontur isobar, cuma dipakai Atmosight

# Dua ini TIDAK diimpor langsung di kode mana pun, tapi tetap WAJIB ada.
# xarray memakainya sebagai mesin pembaca GRIB waktu membuka berkas GFS.
# Kalau dibuang, galatnya baru muncul jauh di dalam saat runtime dan
# bunyinya membingungkan.
cfgrib==0.9.15.1
eccodes==2.47.0
